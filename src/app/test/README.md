# Test Route

This folder owns the dedicated Test Center experience required by the MCP plans.

- `page.tsx` defines the public assessment hub route.
- `TestCenter.module.css` keeps this route's layout and visual rules separate from every other page.

Future chapter quizzes, mock exams, safe-browser flows, and scoring modules should extend from this route instead of being scattered across unrelated pages.
