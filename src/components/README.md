# Components

This folder contains reusable visual components.

- `layout` owns shared shell pieces such as the navbar and footer.
- `learning` owns reusable educational page renderers.
- `ui` owns primitive, reusable interface building blocks.

Components should not contain database or secret-bearing server logic.

