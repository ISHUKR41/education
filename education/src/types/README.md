# Shared Types

This folder contains TypeScript types shared across route handlers, server helpers, and UI boundaries.

Types here should remain stable, intentionally named, and free from direct runtime behavior.

