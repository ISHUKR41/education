# Library Code

This folder contains reusable application logic.

- `curriculum` and `events` store structured domain catalogs.
- `validation` owns reusable request and form schemas.
- `utils` owns framework-light helpers.
- `server` owns backend-only logic and must never be imported into browser-only code.

