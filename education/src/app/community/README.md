# Community Route

This folder owns the student community feed.

- `page.tsx` provides the route shell and lazy client boundary.
- `CommunityClient.tsx` handles filters, posting, and backend data loading.
- `Community.module.css` stores community-only styles.

Future moderation and discussion features should remain coordinated with backend service modules.
